# Bandit configuration for this project
#
# Only disable specific tests that are false positives
# We keep most warnings because they're legitimate security concerns

[bandit]
# Skip B404: subprocess import - The import is safe in context (we use shell=False)
# Skip B602: subprocess shell=True - We fixed this by using shell=False
skips = B404,B602

exclude_dirs = .git,.venv,venv,dbt_packages,target,logs

